# PROJECT_STATUS.md

## สถานะปัจจุบันของโปรเจกต์ Easy Prompt AI

- **ชื่อโปรเจกต์**: Easy Prompt AI (เครื่องมือช่วยสร้างคลิปธรรมะ 1 นาทีด้วย AI)
- **สถาปัตยกรรม**: เว็บไซต์แบบ Static (HTML, CSS, JS) รันบนฝั่งเบราว์เซอร์ 100% ไม่ต้องมีระบบหลังบ้าน
- **โครงสร้างไฟล์ปัจจุบัน**:
  - `index.html`: โครงหน้าเว็บหลัก มีฟอร์มกรอกข้อมูลและส่วนแสดงผลลัพธ์
  - `style.css`: หน้าตาและธีมสีเหลืองทอง/ขาว/น้ำตาล (โทนพุทธศิลป์ อบอุ่น สงบ)
  - `data.js`: ไฟล์เก็บข้อมูลตัวเลือกต่างๆ (กลุ่มเป้าหมาย, สไตล์ภาพ, โทนการเล่า, เสียงบรรยาย, ระดับภาษา)
  - `script.js`: ไฟล์ประมวลผลรับค่าจากฟอร์ม สร้าง Prompt ในแต่ละหมวดหมู่ และแสดงผลการทำงาน
  - `AI_RULES.md`: กฎเกณฑ์และขั้นตอนการปฏิบัติตามของผู้ช่วย AI
  - `PROJECT_STATUS.md`: บันทึกสถานะโปรเจกต์ปัจจุบัน (ไฟล์นี้)
  - `CHANGELOG.md`: ประวัติการเปลี่ยนแปลงแก้ไขโค้ด

## ฟังก์ชันการทำงานที่มีอยู่แล้ว
1. **ส่วนนำเข้าข้อมูล (Form Inputs)**:
   - หัวข้อเรื่อง / ชื่อนิทานชาดก
   - เนื้อเรื่องย่อ / ข้อมูลต้นฉบับ
   - กลุ่มเป้าหมาย (วัยรุ่น, ทั่วไป, พระภิกษุสามเณร, เด็ก, คนทำงาน)
   - สไตล์ภาพ (สมจริงแบบภาพยนตร์, อินเดียโบราณ, ไทยพุทธ, กึ่งภาพวาด, สารคดี)
   - ตั้งค่าเพิ่มเติม (ความยาวคลิป, จำนวนฉาก, โทนการเล่าเรื่อง, เสียงบรรยาย, ระดับภาษา, ชื่อผู้จัดทำ, สถาบัน)
2. **ส่วนประมวลผล (Script Logic)**:
   - ตรวจสอบฟิลด์บังคับ (หัวข้อและเนื้อเรื่องย่อ)
   - คำนวณเวลาต่อฉากตามความยาวและจำนวนฉากที่เลือก
   - ประกอบข้อความภาษาไทยและภาษาอังกฤษเป็นแบบร่างคลิป 1 นาที
3. **ผลลัพธ์ที่ระบบสร้าง (Outputs)**:
   - ชื่อเรื่องคลิป, เรื่องย่อ, สคริปต์เสียงบรรยาย, แผนฉาก, Prompt ภาพ (TH/EN), Prompt วิดีโอ (TH/EN), คำขึ้นจอ/Subtitle, ข้อคิดท้ายเรื่อง, เครดิตท้ายคลิป, Voice Prompt, Master Prompt
4. **ฟังก์ชันเสริมบนหน้าเว็บ**:
   - คัดลอกเฉพาะบางส่วน หรือคัดลอกทั้งหมด (Copy to Clipboard)
   - ดาวน์โหลดผลลัพธ์เป็นไฟล์ข้อความ `.txt`
   - บันทึกโปรเจกต์ลงเครื่องชั่วคราวผ่าน LocalStorage
   - ล้างข้อมูลเพื่อเริ่มเขียนเรื่องใหม่

## การเชื่อมต่อฐานข้อมูล (Database Integration - Phase 2A)
- **Supabase Integration**: เชื่อมต่อ Supabase Project `easy-prompt-ai` ดึงข้อมูลนิทานชาดกจากตาราง `public.stories` สำเร็จ
- **Offline Fallback**: คงระบบ Local Fallback จาก `data.js` ไว้ 100% หากไม่ได้เชื่อมต่อเครือข่าย หรือไม่สามารถดึงข้อมูลจาก Supabase ได้
